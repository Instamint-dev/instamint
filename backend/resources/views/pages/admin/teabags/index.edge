    @let(title ='Tea Bags')
    @include('partials/header')

    <div class="container mt-5">
        <h2>Tea Bags</h2>
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {{params}}
                @each((teaBag) in teaBags)
                <tr>
                    <td>{{ teaBag.id }}</td>
                    {{--  <td>{{ teaBag.name }}</td>  --}}
                    <td><img src="{{ teaBag.image }}" alt=""></td>
                    <td>
                        <form action="{{ route('/teabags', { id: teaBag.id }) }}" method="post" style="display:inline;">
                            {{ csrfField() }}
                            <input type="hidden" name="id" value="{{teaBag.id}}">
                            <button type="submit" class="btn btn-outline-danger">Delete</button>
                        </form>
                    </td>
                </tr>
                @endeach
            </tbody>
        </table>
    </div>
@include('partials/footer')
